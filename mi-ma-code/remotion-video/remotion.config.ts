import { Config } from "@remotion/cli/config";

const config: Config = {
  // 视频输出配置
  defaultProps: {
    // 默认属性
  },
  // 帧率
  fps: 30,
  // 视频尺寸
  width: 1920,
  height: 1080,
  // 输出格式
  logLevel: "verbose",
};

export default config;
